 <nav class="navbar navbar-expand-md navbar-light bg-light spacenav fixed-top">
            <button class="navbar-toggler ml-auto " type="button" data-toggle="collapse"
                    data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <a class="navbar-brand img d-none d-lg-block" href="{{ path('app_index') }}">
                <img width="60" height="60" class="d-inline-block" src="{{ asset('/build/navbar/WCS.png') }}" alt="WCS">
                Wild Series
            </a>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto ">
                    <li class="nav-item dropdown mr-5">
                        <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ 'categories.categories'|trans }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                {{ render(controller('App\\Controller\\DefaultController::navbarTop')) }}
                        </div>
                    </li>
                    <li class="nav-item dropdown mr-5">
                        <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ 'actors.actors'|trans }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="#">Acteur1</a>
                            <a class="dropdown-item" href="#">Ateur2</a>
                            <a class="dropdown-item" href="#">Acteur3</a>
                        </div>
                    </li>
                    <li class="nav-item"><a class="nav-link text-dark" href="#">{{ 'favorites.my_favorites'|trans }}</a></li>
                </ul>
            </div>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto ">
                    <li class="nav-item">
                        <a class="nav-link title-light" href="{{ path('app_index', {'_locale':'fr'}) }}">Fr</a>
                    </li>
                    <li>
                        <a class="nav-link title-light" href="{{ path('app_index', {'_locale':'en'}) }}">En</a>
                    </li>

                        {% if app.user %}
                    <li class="nav-item">
                            <a class="nav-link text-dark mr-4" href="{{ path('app_logout') }}">{{ 'account.log_out'|trans }}</a>
                    </li>
                        {% else %}
                    <li class="nav-item">
                            <a class="nav-link text-dark mr-2" href="{{ path('app_login') }}">{{ 'account.sign_in'|trans }}</a>
                    </li>
                    <li class="nav-item">
                            <a class="nav-link text-dark mr-2" href="{{ path('app_register') }}">{{ 'account.register'|trans }}</a>
                    </li>
                        {% endif %}
                    </li>
                </ul>
                    {% if app.user %}
                        {{ app.user.email }}
                        <a class="navbar-brand img d-none d-lg-block ml-2" href="#">
                            <img width="70" height="70" class="d-inline-block" src="{{ asset('/build/navbar/noun_Male_2997449.png') }}" alt="ID">
                        </a>
                    {% endif %}
            </div>
        </nav>

