{% extends 'base.html.twig' %}
{% block title %}Wild Series{% endblock %}
{% block body %}
    <div class="media">
        <img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }} poster">
        <div class="media-body">
            <h1 class="mt-0">{{ program.title }}</h1>
            <h2>Saison {{ season.number}} de {{ program.title}} : </h2>
            <div>
                <p>Année : {{ season.year }}</p>
                <p>Description : {{ season.description }}</p>
                <ul>Liste des épisodes:
                    {% for episode in season.episodes %}
                        <li><a href="/programs/{{ program.id}}/seasons/{{ season.id }}/episodes/{{ episode.id }}">Episode {{ episode.number }} - {{ episode.title }}</a></li>
                    {% else %}
                        Aucun épisode trouvé.
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>

    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>
{% endblock %}